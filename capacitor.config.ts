import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pt.ipvc.sempreabulir',
  appName: 'SempreABulir',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;